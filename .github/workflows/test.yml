name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app/Contents/Developer

      - name: List available simulators
        run: xcrun simctl list devices available

      - name: Build
        run: xcodebuild -scheme lifetrak -destination 'platform=iOS Simulator,name=iPhone 16' build

      - name: Test
        run: xcodebuild -scheme lifetrak -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:lifetrakTests test
